@model IEnumerable<BookVectorMVC.Models.Book>
@{
    ViewBag.Title = "書籍管理系統";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --success-gradient: linear-gradient(135deg, #00d4aa 0%, #00b894 100%);
            --warning-gradient: linear-gradient(135deg, #f093fb 0%, #ff7675 100%);
            --info-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            --card-shadow: 0 10px 30px rgba(0,0,0,0.1);
            --hover-shadow: 0 15px 40px rgba(0,0,0,0.15);
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }

        .modern-card {
            border: none;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
            background: rgba(255,255,255,0.95);
        }

        .modern-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--hover-shadow);
        }

        .gradient-header {
            background: var(--primary-gradient);
            border-radius: 20px 20px 0 0;
            padding: 2rem;
            color: white;
        }

        .form-control, .form-select {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 12px 16px;
            transition: all 0.3s ease;
        }

        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
            transform: scale(1.02);
        }

        .btn-modern {
            border-radius: 12px;
            padding: 12px 24px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            position: relative;
            overflow: hidden;
        }

        .btn-modern::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn-modern:hover::before {
            left: 100%;
        }

        .btn-modern:hover {
            transform: translateY(-2px);
        }

        .btn-success-modern {
            background: var(--success-gradient);
            color: white;
        }

        .btn-warning-modern {
            background: var(--warning-gradient);
            color: white;
        }

        .btn-info-modern {
            background: var(--info-gradient);
            color: white;
        }

        .stats-badge {
            background: rgba(255,255,255,0.2);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 1rem;
            text-align: center;
            color: white;
        }

        .book-card {
            border: none;
            border-radius: 16px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            background: white;
        }

        .book-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(0,0,0,0.12);
        }

        .animate-fade-in {
            opacity: 1;
            transition: all 0.6s ease-out;
        }

        .title-gradient {
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
        }
    </style>
</head>
<body>
    <div class="container-fluid animate-fade-in">
        <!-- Header -->
        <div class="modern-card mb-5">
            <div class="gradient-header text-center">
                <h1 class="display-4 mb-3">📚 智能書籍管理系統</h1>
                <p class="lead mb-0">運用向量算法，提供精準智能的書籍搜尋體驗</p>
            </div>
        </div>

        <!-- 系統狀態 -->
        <div class="modern-card mb-5">
            <div class="card-header text-white" style="background: var(--info-gradient); border-radius: 20px 20px 0 0;">
                <h5 class="mb-0">📊 系統狀態總覽</h5>
            </div>
            <div class="card-body p-4">
                @if (ViewBag.Statistics != null)
                {
                    var stats = ViewBag.Statistics as Dictionary<string, object>;
                    <div class="row g-4">
                        <div class="col-md-3">
                            <div class="stats-badge" style="background: var(--primary-gradient);">
                                <h3 class="mb-2">@(stats.ContainsKey("TotalBooks") ? stats["TotalBooks"] : 0)</h3>
                                <small>📚 總書籍數</small>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="stats-badge" style="background: var(--success-gradient);">
                                <h3 class="mb-2">@(stats.ContainsKey("BooksWithVectors") ? stats["BooksWithVectors"] : 0)</h3>
                                <small>🧠 已向量化</small>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="stats-badge" style="background: var(--warning-gradient);">
                                <h3 class="mb-2">@(stats.ContainsKey("UniquePositions") ? stats["UniquePositions"] : 0)</h3>
                                <small>📍 儲存位置</small>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="stats-badge" style="background: var(--info-gradient);">
                                <h3 class="mb-2">@(stats.ContainsKey("AvgVectorDimension") ? Math.Round((double)stats["AvgVectorDimension"], 1) : 0)</h3>
                                <small>📏 平均維度</small>
                            </div>
                        </div>
                    </div>
                }
                else
                {
                    <div class="text-center text-muted">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">載入中...</span>
                        </div>
                        <p class="mt-3">載入統計資料中...</p>
                    </div>
                }
            </div>
        </div>

        <div class="row g-4 mb-5">
            <!-- 左側：新增書籍 -->
            <div class="col-lg-6">
                <div class="modern-card h-100">
                    <div class="card-header text-white" style="background: var(--success-gradient); border-radius: 20px 20px 0 0;">
                        <h5 class="mb-0">📖 新增書籍</h5>
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm("Add", "EnhancedBook", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate" }))
                        {
                            <div class="mb-3">
                                <label class="form-label">📖 書籍標題 *</label>
                                <input name="title" class="form-control" placeholder="請輸入書籍標題" required />
                                <div class="invalid-feedback">請輸入書籍標題</div>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label">📝 書籍描述</label>
                                <textarea name="description" class="form-control" rows="3" placeholder="請輸入書籍描述（選填）"></textarea>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label">📍 書籍位置</label>
                                <input name="position" class="form-control" placeholder="例如: A-12, B-05" />
                                <div class="form-text">書架標籤或位置編號</div>
                            </div>

                            <button type="submit" class="btn btn-modern btn-success-modern w-100">
                                ➕ 新增書籍
                            </button>
                        }
                        
                    </div>
                </div>
            </div>

            <!-- 右側：搜尋功能 -->
            <div class="col-lg-6">
                <div class="modern-card h-100">
                    <div class="card-header text-white" style="background: var(--warning-gradient); border-radius: 20px 20px 0 0;">
                        <h5 class="mb-0">🔍 智能搜尋</h5>
                    </div>
                    <div class="card-body">
                        <form id="searchForm">
                            <div class="mb-3">
                                <label class="form-label">🎯 關鍵字搜尋 (向量相似度)</label>
                                <input id="searchQuery" class="form-control" placeholder="請輸入搜尋關鍵字" />
                                <div class="form-text">使用向量算法找出最相關的書籍</div>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label">📍 位置搜尋 (精確匹配)</label>
                                <input id="searchPosition" class="form-control" placeholder="例如: A-12" />
                                <div class="form-text">根據書架位置精確查找</div>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label">📖 書名搜尋 (模糊匹配)</label>
                                <input id="searchTitle" class="form-control" placeholder="請輸入書名關鍵字" />
                                <div class="form-text">根據書名進行模糊查詢</div>
                            </div>

                            <div class="d-grid gap-2">
                                <button type="button" id="btnSearch" class="btn btn-modern btn-warning-modern">
                                    🔍 開始搜尋
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!-- 搜尋結果區域 -->
        <div class="row mt-4">
            <div class="col-12">
                <div id="searchResultArea"></div>
            </div>
        </div>

        <!-- 書籍列表 -->
        <div class="row">
            <div class="col-12">
                <div class="modern-card">
                    <div class="card-header text-white" style="background: linear-gradient(135deg, #6c757d 0%, #495057 100%); border-radius: 20px 20px 0 0;">
                        <h5 class="mb-0">📋 總書籍清單</h5>
                    </div>
                    <div class="card-body">
                        @if (Model != null && Model.Any())
                        {
                            <div class="table-responsive">
                                <table class="table table-hover align-middle">
                                    <thead style="background: var(--primary-gradient); color: white;">
                                        <tr class="text-center">
                                            <th style="border-radius: 12px 0 0 0;">ID</th>
                                            <th>📚 書名</th>
                                            <th>📝 描述</th>
                                            <th>📍 位置</th>
                                            <th style="border-radius: 0 12px 0 0;">🧠 向量狀態</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var book in Model)
                                        {
                                            <tr>
                                                <td class="text-center">@book.BookId</td>
                                                <td><strong>@book.Title</strong></td>
                                                <td>
                                                    <div style="max-width: auto; max-height: 60px; overflow-y: auto;">
                                                        @book.Description
                                                    </div>
                                                </td>
                                                <td class="text-center">
                                                    <span class="badge rounded-pill px-3 py-2" style="background: var(--info-gradient); color: white;">
                                                        @book.Position
                                                    </span>
                                                </td>
                                                <td class="text-center">
                                                    @if (!string.IsNullOrEmpty(book.Vector) && book.Vector != "[]")
                                                    {
                                                        <span class="badge rounded-pill px-3 py-2" style="background: var(--success-gradient); color: white;">
                                                            ✓ 已向量化
                                                        </span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge rounded-pill px-3 py-2" style="background: var(--warning-gradient); color: white;">
                                                            ⚠ 待處理
                                                        </span>
                                                    }
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <div class="text-center p-5">
                                <div class="mb-4">
                                    <div style="font-size: 4rem; opacity: 0.3;">📚</div>
                                </div>
                                <h5 class="text-muted mb-3">目前沒有書籍資料</h5>
                                <p class="text-muted">請使用上方表單新增您的第一本書籍</p>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 通知 -->
    <div class="position-fixed top-0 end-0 p-3" style="z-index: 9999;">
        <div id="toastContainer"></div>
    </div>

    @if (TempData["Success"] != null)
    {
        <script>
            $(document).ready(function() {
                showToast('success', '成功', '@TempData["Success"]');
            });
        </script>
    }

    @if (TempData["Error"] != null)
    {
        <script>
            $(document).ready(function() {
                showToast('error', '錯誤', '@TempData["Error"]');
            });
        </script>
    }

    <script>
        $(document).ready(function () {
            // 搜尋功能
            $('#btnSearch').click(function () {
                const query = $('#searchQuery').val();
                const position = $('#searchPosition').val();
                const title = $('#searchTitle').val();

                if (!query && !position && !title) {
                    alert('請至少輸入一個搜尋條件');
                    return;
                }

                $(this).prop('disabled', true).html('🔍 搜尋中...');

                $.post('/EnhancedBook/Search', {
                    query: query,
                    position: position,
                    title: title
                }, function (html) {
                    $('#searchResultArea').html(html);
                    $('#btnSearch').prop('disabled', false).html('🔍 開始搜尋');
                }).fail(function () {
                    alert('搜尋時發生錯誤');
                    $('#btnSearch').prop('disabled', false).html('🔍 開始搜尋');
                });
            });


            // 表單驗證
            $('form.needs-validation').on('submit', function (e) {
                if (!this.checkValidity()) {
                    e.preventDefault();
                    e.stopPropagation();
                }
                $(this).addClass('was-validated');
            });
        });

        function showToast(type, title, message) {
            const toastId = 'toast_' + Date.now();
            const bgClass = type === 'success' ? 'success' : 'danger';
            const icon = type === 'success' ? '✅' : '❌';
            
            const toastHtml = `
                <div id="${toastId}" class="toast" role="alert" style="background: var(--${type === 'success' ? 'success' : 'warning'}-gradient); color: white; border: none; border-radius: 12px;">
                    <div class="toast-header" style="background: rgba(255,255,255,0.1); border: none; border-radius: 12px 12px 0 0;">
                        <span class="me-2">${icon}</span>
                        <strong class="me-auto" style="color: white;">${title}</strong>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
                    </div>
                    <div class="toast-body">
                        ${message}
                    </div>
                </div>
            `;
            
            $('#toastContainer').append(toastHtml);
            const toast = new bootstrap.Toast(document.getElementById(toastId));
            toast.show();
            
            // 自動移除DOM
            setTimeout(function() {
                $('#' + toastId).remove();
            }, 6000);
        }
    </script>
</body>
</html>